classes: {
  {% for icon in icons_list %}
  {% set name = icon | replace('.svg', '') %}
    {{- name }}: {
        icon: {{ url }}/{{ icon }}
        shape: image
        label: "{{ name }}"
      }
  {% endfor %}
}